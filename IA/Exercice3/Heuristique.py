"""
Calculs des différents heuristiques pour le modèle d'AI
"""

def GainMoyen(S: list[str],w: str , Swx: list[str] = None) -> int:
    """
    Calcul du gain moyen de w par rapport à S
    param :
        - S : liste de mots possibles
        - w : mot proposé
        - Swx : liste de mots possibles après comparaison de w avec les x de S (optionnel)
    return :
        - gain moyen de w par rapport à S
    """
    return

def GainMinimum(S: list[str],w: str , Swx: list[str] = None) -> int:
    """
    Calcul du gain minimum de w par rapport à S
    param :
        - S : liste de mots possibles
        - w : mot proposé
        - Swx : liste de mots possibles après comparaison de w avec les x de S (optionnel)
    return :
        - gain minimum de w par rapport à S
    """
    return

